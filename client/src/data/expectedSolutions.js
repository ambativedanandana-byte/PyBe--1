const EXPECTED_SOLUTIONS = {
  'b-syn-1': {
    expectedCode: [
      '# Bakery Order System',
      'def calculate_price(item, quantity):',
      '    price = quantity * 2.50',
      '    print(f"{item}: ${price}")',
      '    return price',
      '',
      'def print_total():',
      '    total = calculate_price("cupcakes", 24)',
      '    total = total + calculate_price("croissants", 12)',
      '    print(f"Total: ${total}")',
      '',
      'print_total()',
    ],
    expectedOutput: 'cupcakes: $60.0\ncroissants: $30.0\nTotal: $90.0',
    expectedBehavior: 'The program should define two functions, calculate the price for each bakery order, and print the total cost.',
  },
  'b-syn-2': {
    expectedCode: [
      '# Library Book Organizer',
      'sections = ["fiction", "non-fiction", "reference"]',
      '',
      'for section in sections:',
      '    print(f"Setting up: {section}")',
      '    books = get_books(section)',
      '    for book in books:',
      '        place_on_shelf(book, section)',
      '',
      'print("All sections organized!")',
    ],
    expectedOutput: 'Setting up: fiction\nSetting up: non-fiction\nSetting up: reference\nAll sections organized!',
    expectedBehavior: 'The program should loop through each library section, print which section is being set up, and organize books into each section.',
  },
  'b-syn-3': {
    expectedCode: [
      '# Phone Contact List',
      'contacts = ["Alice", "Bob", "Carol",',
      '            "Dave", "Emily", "Frank",',
      '            "Grace", "Henry"]',
      '',
      'print(f"Total contacts: {len(contacts)}")',
      '',
      'for name in contacts:',
      '    print(f"Calling {name}...")',
    ],
    expectedOutput: 'Total contacts: 8\nCalling Alice...\nCalling Bob...\nCalling Carol...\nCalling Dave...\nCalling Emily...\nCalling Frank...\nCalling Grace...\nCalling Henry...',
    expectedBehavior: 'The program should store a list of 8 contacts, print the total count, and then loop through each name printing a calling message.',
  },
  'b-syn-4': {
    expectedCode: [
      '# Travel Journal Entry',
      'entry = "I visited the most wonderful place today"',
      'review = "The pasta was delicious"',
      '',
      'quote = \'The waiter said, "The Chef\'s special is amazing"\'',
      '',
      'print(entry)',
      'print(review)',
      'print(quote)',
    ],
    expectedOutput: 'I visited the most wonderful place today\nThe pasta was delicious\nThe waiter said, "The Chef\'s special is amazing"',
    expectedBehavior: 'The program should store a journal entry, a review, and a quote with nested quotation marks, then print all three.',
  },
  'b-syn-5': {
    expectedCode: [
      '# Science Lab Instructions',
      'def measure_volume(amount):',
      '    print(f"Measuring {amount}ml of solution")',
      '    return amount',
      '',
      'def observe_color(mixture):',
      '    print(f"Observing color change in {mixture}")',
      '    return "color_changed"',
      '',
      'solution = measure_volume(50)',
      'result = observe_color(solution)',
      'print(f"Experiment complete: {result}")',
    ],
    expectedOutput: 'Measuring 50ml of solution\nObserving color change in 50\nExperiment complete: color_changed',
    expectedBehavior: 'The program should define correctly-spelled functions to measure volume and observe color, then run the experiment steps.',
  },
  'b-syn-6': {
    expectedCode: [
      '# Grocery List Manager',
      'items = ["eggs",',
      '         "milk",',
      '         "bread",',
      '         "butter",',
      '         "chicken",',
      '         "rice"]',
      '',
      'print(f"Shopping list ({len(items)} items):")',
      'for item in items:',
      '    print(f"  - {item}")',
    ],
    expectedOutput: 'Shopping list (6 items):\n  - eggs\n  - milk\n  - bread\n  - butter\n  - chicken\n  - rice',
    expectedBehavior: 'The program should store a list of 6 grocery items with proper separators, print the count, and list each item.',
  },
  'b-syn-7': {
    expectedCode: [
      '# Concert Ticket Form',
      'name = input("Enter your name: ")',
      'email = input("Enter your email: ")',
      'tickets = int(input("Number of tickets: "))',
      '',
      'order = {',
      '    "name": name,',
      '    "email": email,',
      '    "tickets": tickets',
      '}',
      '',
      'print(f"Order confirmed: {order}")',
    ],
    expectedOutput: 'Order confirmed: {\'name\': \'John\', \'email\': \'john@example.com\', \'tickets\': 2}',
    expectedBehavior: 'The program should collect user input for name, email, and ticket count, store them in a dictionary with proper separators, and print the order confirmation.',
  },
  'b-run-1': {
    expectedCode: [
      '# School Awards Ceremony',
      'awards = {',
      '    "Best Science": "Alexandra Chen",',
      '    "Best Math": "TBD",',
      '    "Best Art": "Jordan Lee",',
      '    "Best Music": "TBD",',
      '    "Best Sports": "Sam Rivera"',
      '}',
      '',
      'for category, student in awards.items():',
      '    print(f"{category}: {student}")',
    ],
    expectedOutput: 'Best Science: Alexandra Chen\nBest Math: TBD\nBest Art: Jordan Lee\nBest Music: TBD\nBest Sports: Sam Rivera',
    expectedBehavior: 'The program should store award categories with student names (using valid Python values instead of undefined), then print each award.',
  },
  'b-run-2': {
    expectedCode: [
      '# Apple Pie Recipe',
      'flour_cups = 2',
      'butter_cups = flour_cups / 2',
      '',
      'print(f"Flour: {flour_cups} cups")',
      'print(f"Butter: {butter_cups} cups")',
      'print("Mix ingredients together")',
    ],
    expectedOutput: 'Flour: 2 cups\nButter: 1.0 cups\nMix ingredients together',
    expectedBehavior: 'The program should calculate the butter amount by dividing flour by a valid number (not zero), then print the recipe instructions.',
  },
  'b-run-3': {
    expectedCode: [
      '# Train Schedule App',
      'def get_platform(station):',
      '    stations = {"NYC": 1, "BOS": 3, "CHI": 5}',
      '    return stations.get(station, "Unknown")',
      '',
      'departure = "NYC"',
      'arrival = "BOS"',
      '',
      'print(f"Platform for {arrival}: {get_platform(arrival)}")',
    ],
    expectedOutput: 'Platform for BOS: 3',
    expectedBehavior: 'The program should look up a station in the dictionary and return its platform number, handling cases where the station might not exist.',
  },
  'b-run-4': {
    expectedCode: [
      '# Voting System',
      'def cast_vote(choice):',
      '    if choice == 1:',
      '        return "Candidate A"',
      '    elif choice == 2:',
      '        return "Candidate B"',
      '    elif choice == 3:',
      '        return "Candidate C"',
      '    else:',
      '        return "Invalid choice"',
      '',
      'voter_choice = 2',
      'result = cast_vote(voter_choice)',
      'print(f"You voted for: {result}")',
    ],
    expectedOutput: 'You voted for: Candidate B',
    expectedBehavior: 'The program should accept a vote choice, handle valid and invalid inputs, and return the correct candidate name.',
  },
  'b-log-1': {
    expectedCode: [
      '# Pizza Party Calculator',
      'people = 24',
      'slices_per_person = 3',
      'slices_per_pizza = 8',
      '',
      'total_slices = people * slices_per_person',
      'pizzas_needed = total_slices / slices_per_pizza',
      '',
      'print(f"People: {people}")',
      'print(f"Slices needed: {total_slices}")',
      'print(f"Pizzas to order: {pizzas_needed}")',
    ],
    expectedOutput: 'People: 24\nSlices needed: 72\nPizzas to order: 9.0',
    expectedBehavior: 'The program should calculate total slices needed and then divide by slices per pizza to get the number of pizzas (not multiply).',
  },
  'b-log-2': {
    expectedCode: [
      '# Smart Thermostat',
      'current_temp = 68',
      'target_temp = 72',
      '',
      'if current_temp < target_temp:',
      '    print("Heater: ON")',
      'else:',
      '    print("Heater: OFF")',
      '',
      'print(f"Current: {current_temp}°F")',
      'print(f"Target: {target_temp}°F")',
    ],
    expectedOutput: 'Heater: ON\nCurrent: 68°F\nTarget: 72°F',
    expectedBehavior: 'The program should turn the heater ON when the current temperature is below the target (use < not >).',
  },
  'b-log-3': {
    expectedCode: [
      '# Student Grade Calculator',
      'ryan_scores = [85, 92, 78, 90]',
      'other_scores = [70, 75, 80, 85]',
      '',
      'def calculate_average(scores):',
      '    return sum(scores) / len(scores)',
      '',
      'ryan_avg = calculate_average(ryan_scores)',
      'other_avg = calculate_average(other_scores)',
      '',
      'print(f"Ryan\'s average: {ryan_avg}")',
      'print(f"Other average: {other_avg}")',
    ],
    expectedOutput: 'Ryan\'s average: 86.25\nOther average: 77.5',
    expectedBehavior: 'The program should calculate the average of each student\'s scores using the correct variable for each student.',
  },
  'b-log-4': {
    expectedCode: [
      '# Traffic Light Controller',
      'is_daytime = True',
      'is_nighttime = False',
      '',
      'if is_daytime:',
      '    print("Light: GREEN")',
      'elif is_nighttime:',
      '    print("Light: RED")',
      '',
      'print("Traffic flowing normally")',
    ],
    expectedOutput: 'Light: GREEN\nTraffic flowing normally',
    expectedBehavior: 'The program should handle both daytime (GREEN) and nighttime (RED) conditions, not just daytime.',
  },
  'e-syn-1': {
    expectedCode: [
      '# Nested Loop Organizer',
      'rooms = ["kitchen", "living room", "bedroom"]',
      '',
      'for room in rooms:',
      '    items = get_items(room)',
      '    for item in items:',
      '        print(f"Checking {item} in {room}")',
      '    print(f"{room} organized")',
    ],
    expectedOutput: 'Checking item in kitchen\nkitchen organized\nChecking item in living room\nliving room organized\nChecking item in bedroom\nbedroom organized',
    expectedBehavior: 'The program should loop through rooms and items with correct indentation, checking each item in each room.',
  },
  'e-syn-2': {
    expectedCode: [
      '# Function Definition Error',
      'def greet_user(name):',
      '    """Greet the user"""',
      '    print(f"Hello, {name}!")',
      '    return f"Welcome, {name}"',
      '',
      'message = greet_user("Alice")',
      'print(message)',
    ],
    expectedOutput: 'Hello, Alice!\nWelcome, Alice',
    expectedBehavior: 'The program should define a function with proper syntax (colon after parameters), call it, and print the greeting and return value.',
  },
  'e-syn-3': {
    expectedCode: [
      '# Collection Manager',
      'fruits = ["apple", "banana", "cherry"]',
      'vegetables = ["carrot", "pea", "corn"]',
      '',
      'print(f"Fruits: {len(fruits)}")',
      'print(f"Vegetables: {len(vegetables)}")',
    ],
    expectedOutput: 'Fruits: 3\nVegetables: 3',
    expectedBehavior: 'The program should define two separate lists with proper closing brackets, then print the count of each.',
  },
  'e-syn-4': {
    expectedCode: [
      '# Variable Assignment Error',
      'x = 10',
      'y = 20',
      '',
      'sum_xy = x + y',
      '',
      'print(f"Sum: {sum_xy}")',
    ],
    expectedOutput: 'Sum: 30',
    expectedBehavior: 'The program should assign the result of x + y to a variable (not assign to an expression), then print the sum.',
  },
  'e-run-1': {
    expectedCode: [
      '# List Index Accessor',
      'colors = ["red", "green", "blue"]',
      '',
      'print(f"First color: {colors[0]}")',
      'print(f"Second color: {colors[1]}")',
      'print(f"Third color: {colors[2]}")',
    ],
    expectedOutput: 'First color: red\nSecond color: green\nThird color: blue',
    expectedBehavior: 'The program should access list elements by valid index values (0, 1, 2) without going out of range.',
  },
  'e-run-2': {
    expectedCode: [
      '# Dictionary Lookup',
      'person = {"name": "Alice", "age": 25, "city": "New York"}',
      '',
      'print(f"Name: {person[\'name\']}")',
      'print(f"Age: {person[\'age\']}")',
      'print(f"City: {person[\'city\']}")',
    ],
    expectedOutput: 'Name: Alice\nAge: 25\nCity: New York',
    expectedBehavior: 'The program should include all needed keys in the dictionary so lookups succeed without KeyError.',
  },
  'e-run-3': {
    expectedCode: [
      '# String Method Caller',
      'text = "Hello World"',
      '',
      'print(text.upper())',
      'print(text.lower())',
      'print(text[::-1])',
    ],
    expectedOutput: 'HELLO WORLD\nhello world\ndlroW olleH',
    expectedBehavior: 'The program should use valid string methods — upper(), lower(), and slicing for reversal (not the nonexistent reverse() method).',
  },
  'e-run-4': {
    expectedCode: [
      '# File Reader',
      'filename = "data.txt"',
      '',
      'try:',
      '    with open(filename, "r") as file:',
      '        content = file.read()',
      '        print(content)',
      'except FileNotFoundError:',
      '    print(f"File not found: {filename}")',
    ],
    expectedOutput: 'File not found: data.txt',
    expectedBehavior: 'The program should attempt to read a file and handle the case where the file does not exist gracefully.',
  },
  'e-run-5': {
    expectedCode: [
      '# Module Importer',
      'import math',
      'import statistics',
      '',
      'print(math.sqrt(16))',
      'print(statistics.mean([1, 2, 3, 4, 5]))',
    ],
    expectedOutput: '4.0\n3.0',
    expectedBehavior: 'The program should import only modules that exist and use them to perform calculations.',
  },
  'e-run-6': {
    expectedCode: [
      '# Package Importer',
      'from datetime import datetime',
      'from os import path',
      '',
      'now = datetime.now()',
      'print(f"Current time: {now}")',
    ],
    expectedOutput: 'Current time: 2024-01-15 10:30:00.123456',
    expectedBehavior: 'The program should import from packages that exist and use them correctly.',
  },
  'e-log-1': {
    expectedCode: [
      '# Loop Counter',
      'count = 0',
      'for i in range(10):',
      '    count = i + 1',
      '',
      'print(f"Final count: {count}")',
      'print(f"Expected: 10")',
    ],
    expectedOutput: 'Final count: 10\nExpected: 10',
    expectedBehavior: 'The program should count from 1 to 10 (not 0 to 9) so the final count matches the expected value of 10.',
  },
  'e-log-2': {
    expectedCode: [
      '# Counter Program',
      'x = 1',
      '',
      'while x > 0:',
      '    print(f"x = {x}")',
      '    x = x - 1',
    ],
    expectedOutput: 'x = 1',
    expectedBehavior: 'The program should decrement x so the loop eventually terminates (not increment which causes an infinite loop).',
  },
  'e-log-3': {
    expectedCode: [
      '# Range Iterator',
      'for i in range(5):',
      '    print(f"Processing item {i}")',
      '',
      'print(f"Total items processed: {i + 1}")',
    ],
    expectedOutput: 'Processing item 0\nProcessing item 1\nProcessing item 2\nProcessing item 3\nProcessing item 4\nTotal items processed: 5',
    expectedBehavior: 'The program should report the total number of items processed as 5 (range(5) produces 0-4, so add 1).',
  },
  'e-log-4': {
    expectedCode: [
      '# Boolean Logic Checker',
      'age = 20',
      'has_ticket = True',
      '',
      'if age >= 18 and has_ticket == True:',
      '    print("Entry allowed")',
      'else:',
      '    print("Entry denied")',
    ],
    expectedOutput: 'Entry allowed',
    expectedBehavior: 'The program should use == for comparison (not =) to check if the boolean condition is true.',
  },
  'e-log-5': {
    expectedCode: [
      '# Calculation Order',
      'a = 10',
      'b = 5',
      'c = 2',
      '',
      'result = (a + b) * c',
      'print(f"Result: {result}")',
      '',
      'expected = (a + b) * c',
      'print(f"Expected: {expected}")',
    ],
    expectedOutput: 'Result: 30\nExpected: 30',
    expectedBehavior: 'The program should use parentheses to ensure addition happens before multiplication, matching the expected result.',
  },
  'bu-syn-1': {
    expectedCode: [
      '# Multi-Issue Program',
      'def process_data(data):',
      '    result = []',
      '    for item in data:',
      '        if item > 0:',
      '            result.append(item * 2)',
      '    return result',
      '',
      'numbers = [1, -2, 3, -4, 5]',
      'processed = process_data(numbers)',
      'print(processed)',
    ],
    expectedOutput: '[2, 6, 10]',
    expectedBehavior: 'The program should fix all syntax issues (missing colons, missing closing parenthesis) and process positive numbers by doubling them.',
  },
  'bu-run-1': {
    expectedCode: [
      '# Multiple Exception Program',
      'data = {"a": 1, "b": 2}',
      '',
      'result = data.get("a", 0)',
      'value = result / 1 if result != 0 else 0',
      'text = str(value)',
      'print(text)',
    ],
    expectedOutput: '1',
    expectedBehavior: 'The program should safely access dictionary values, avoid division by zero, and convert the result to a string.',
  },
  'bu-run-2': {
    expectedCode: [
      '# Exception Handling',
      'def safe_divide(a, b):',
      '    try:',
      '        result = a / b',
      '        return result',
      '    except (ZeroDivisionError, TypeError):',
      '        return "Error"',
      '',
      'print(safe_divide(10, 2))',
      'print(safe_divide(10, 0))',
      'print(safe_divide(10, "a"))',
    ],
    expectedOutput: '5.0\nError\nError',
    expectedBehavior: 'The program should handle division by zero and type errors gracefully, returning an error message instead of crashing.',
  },
  'bu-run-3': {
    expectedCode: [
      '# User Input Handler',
      'user_input = "42"',
      'try:',
      '    number = int(user_input)',
      '    result = 100 / number',
      '    print(f"Result: {result}")',
      'except ValueError:',
      '    print("Invalid number")',
      'except ZeroDivisionError:',
      '    print("Cannot divide by zero")',
    ],
    expectedOutput: 'Result: 2.380952380952381',
    expectedBehavior: 'The program should handle invalid input and division by zero with try/except blocks.',
  },
  'bu-log-1': {
    expectedCode: [
      '# Search Algorithm',
      'def linear_search(arr, target):',
      '    for i in range(len(arr)):',
      '        if arr[i] == target:',
      '            return i',
      '    return -1',
      '',
      'numbers = [10, 20, 30, 40, 50]',
      'index = linear_search(numbers, 30)',
      'print(f"Found at index: {index}")',
    ],
    expectedOutput: 'Found at index: 2',
    expectedBehavior: 'The program should search through a list and return the correct index of the target element.',
  },
  'bu-log-2': {
    expectedCode: [
      '# Search Validator',
      'def validate_search(query, database):',
      '    if query.lower() in [item.lower() for item in database]:',
      '        return True',
      '    return False',
      '',
      'database = ["apple", "banana", "cherry"]',
      'print(validate_search("Apple", database))',
    ],
    expectedOutput: 'True',
    expectedBehavior: 'The program should perform case-insensitive search so "Apple" matches "apple" in the database.',
  },
  'bu-log-3': {
    expectedCode: [
      '# Input Validator',
      'def validate_age(age):',
      '    if 0 < age <= 120:',
      '        return True',
      '    return False',
      '',
      'print(validate_age(25))',
      'print(validate_age(-5))',
      'print(validate_age(150))',
    ],
    expectedOutput: 'True\nFalse\nFalse',
    expectedBehavior: 'The program should validate that age is between 1 and 120 (not just greater than 0).',
  },
  'bu-log-4': {
    expectedCode: [
      '# Bubble Sort',
      'def bubble_sort(arr):',
      '    n = len(arr)',
      '    for i in range(n):',
      '        for j in range(0, n-i-1):',
      '            if arr[j] > arr[j+1]:',
      '                arr[j], arr[j+1] = arr[j+1], arr[j]',
      '    return arr',
      '',
      'numbers = [64, 34, 25, 12, 22, 11, 90]',
      'sorted_numbers = bubble_sort(numbers)',
      'print(sorted_numbers)',
    ],
    expectedOutput: '[11, 12, 22, 25, 34, 64, 90]',
    expectedBehavior: 'The program should sort the array in ascending order using the bubble sort algorithm.',
  },
  'bu-log-5': {
    expectedCode: [
      '# Temperature Converter',
      'def celsius_to_fahrenheit(celsius):',
      '    return celsius * 9 / 5 + 32',
      '',
      'def fahrenheit_to_celsius(fahrenheit):',
      '    return (fahrenheit - 32) * 5 / 9',
      '',
      'print(f"100°C = {celsius_to_fahrenheit(100)}°F")',
      'print(f"32°F = {fahrenheit_to_celsius(32)}°C")',
    ],
    expectedOutput: '100°C = 212.0°F\n32°F = 0.0°C',
    expectedBehavior: 'The program should convert temperatures correctly — the Fahrenheit to Celsius formula should subtract 32 first.',
  },
  'bu-log-6': {
    expectedCode: [
      '# Edge Case Handler',
      'def find_max(numbers):',
      '    if not numbers:',
      '        return None',
      '    max_val = numbers[0]',
      '    for num in numbers:',
      '        if num > max_val:',
      '            max_val = num',
      '    return max_val',
      '',
      'print(find_max([3, 7, 2, 8, 1]))',
      'print(find_max([]))',
    ],
    expectedOutput: '8\nNone',
    expectedBehavior: 'The program should handle empty lists by checking before accessing index 0, returning None for empty input.',
  },
};

export function getExpectedSolution(id) {
  return EXPECTED_SOLUTIONS[id] || null;
}

export default EXPECTED_SOLUTIONS;
