const EXPECTED_SOLUTIONS = {
  'b-syn-1': {
    expectedCode: [
      'def greet():',
      '    print("Hello!")',
      'def add(a, b):',
      '    return a + b',
      '',
      'greet()',
      'result = add(2, 3)',
      'print(result)',
    ],
    expectedOutput: 'Hello!\n5',
    expectedBehavior: 'Add a colon after the function definition to complete the syntax.',
  },
  'b-syn-2': {
    expectedCode: [
      'colors = ["red", "green", "blue"]',
      '',
      'for color in colors:',
      '    print(color)',
    ],
    expectedOutput: 'red\ngreen\nblue',
    expectedBehavior: 'Add a colon after the for loop header to complete the syntax.',
  },
  'b-syn-3': {
    expectedCode: [
      'fruits = ["apple", "banana",',
      '          "cherry", "grape"]',
      '',
      'print(fruits)',
    ],
    expectedOutput: "['apple', 'banana', 'cherry', 'grape']",
    expectedBehavior: 'Add a comma between "banana" and "cherry" to separate the items.',
  },
  'b-syn-4': {
    expectedCode: [
      'name = "Alice"',
      'age = 25',
      '',
      'print(name)',
      'print(age)',
    ],
    expectedOutput: 'Alice\n25',
    expectedBehavior: 'This code has no syntax errors and runs correctly.',
  },
  'b-syn-5': {
    expectedCode: [
      'def measure(amount):',
      '    print(f"Measuring {amount}ml")',
      '    return amount',
      '',
      'x = measure(50)',
      'print(x)',
    ],
    expectedOutput: 'Measuring 50ml\n50',
    expectedBehavior: 'The code works but uses non-standard function names. No fix needed.',
  },
  'b-syn-6': {
    expectedCode: [
      'items = ["eggs",',
      '         "milk",',
      '         "bread"]',
      '',
      'print(items)',
    ],
    expectedOutput: "['eggs', 'milk', 'bread']",
    expectedBehavior: 'Add commas between list items to separate them properly.',
  },
  'b-syn-7': {
    expectedCode: [
      'order = {',
      '    "name": "Alice",',
      '    "item": "cake"',
      '}',
      '',
      'print(order)',
    ],
    expectedOutput: "{'name': 'Alice', 'item': 'cake'}",
    expectedBehavior: 'Add a comma after the first dictionary entry.',
  },
  'b-run-1': {
    expectedCode: [
      'names = {',
      '    "A": "Alice",',
      '    "B": "Bob",',
      '    "C": "Carol"',
      '}',
      '',
      'for key in names:',
      '    print(names[key])',
    ],
    expectedOutput: 'Alice\nBob\nCarol',
    expectedBehavior: 'Replace "undefined" with an actual value like "Bob".',
  },
  'b-run-2': {
    expectedCode: [
      'total = 10',
      'share = total / 2',
      '',
      'print(share)',
    ],
    expectedOutput: '5.0',
    expectedBehavior: 'Change the divisor from 0 to a non-zero number.',
  },
  'b-run-3': {
    expectedCode: [
      'data = {"NYC": 1, "BOS": 3}',
      '',
      'city = "NYC"',
      'print(data[city])',
    ],
    expectedOutput: '1',
    expectedBehavior: 'Use a key that exists in the dictionary.',
  },
  'b-run-4': {
    expectedCode: [
      'vote = input("Pick 1, 2, or 3: ")',
      '',
      'if vote == "1":',
      '    print("A")',
      'elif vote == "2":',
      '    print("B")',
      'else:',
      '    print("C")',
    ],
    expectedOutput: '',
    expectedBehavior: 'Compare the input as a string ("1") instead of a number (1).',
  },
  'b-log-1': {
    expectedCode: [
      'people = 24',
      'slices_each = 3',
      'slices_per_pizza = 8',
      '',
      'total = people * slices_each',
      'pizzas = total / slices_per_pizza',
      '',
      'print(f"Pizzas needed: {pizzas}")',
    ],
    expectedOutput: 'Pizzas needed: 9.0',
    expectedBehavior: 'Use division instead of multiplication to calculate pizzas needed.',
  },
  'b-log-2': {
    expectedCode: [
      'current = 68',
      'target = 72',
      '',
      'if current < target:',
      '    print("Heat ON")',
      'else:',
      '    print("Heat OFF")',
    ],
    expectedOutput: 'Heat ON',
    expectedBehavior: 'Change > to < so the heater turns on when current is below target.',
  },
  'b-log-3': {
    expectedCode: [
      'scores_a = [85, 92, 78, 90]',
      'scores_b = [70, 75, 80, 85]',
      '',
      'avg_a = sum(scores_a) / len(scores_a)',
      'avg_b = sum(scores_b) / len(scores_b)',
      '',
      'print(f"A: {avg_a}")',
      'print(f"B: {avg_b}")',
    ],
    expectedOutput: 'A: 86.25\nB: 77.5',
    expectedBehavior: 'Use scores_b in the second calculation instead of scores_a.',
  },
  'b-log-4': {
    expectedCode: [
      'is_day = True',
      '',
      'if is_day:',
      '    print("GREEN")',
      'else:',
      '    print("RED")',
      '',
      'print("Go")',
    ],
    expectedOutput: 'GREEN\nGo',
    expectedBehavior: 'Add an else branch to handle nighttime with a different light.',
  },
  'e-syn-1': {
    expectedCode: [
      'rooms = ["kitchen", "bedroom"]',
      '',
      'for room in rooms:',
      '    items = ["table", "chair"]',
      '    for item in items:',
      '        print(item)',
    ],
    expectedOutput: 'table\nchair\ntable\nchair',
    expectedBehavior: 'Fix the indentation of the inner for loop to align with the outer loop.',
  },
  'e-syn-2': {
    expectedCode: [
      'def greet(name):',
      '    print(f"Hello {name}")',
      '',
      'greet("Alice")',
    ],
    expectedOutput: 'Hello Alice',
    expectedBehavior: 'Add a colon after the function parameters.',
  },
  'e-syn-3': {
    expectedCode: [
      'fruits = ["apple", "banana"]',
      'vegs = ["carrot", "pea"]',
      '',
      'print(fruits)',
      'print(vegs)',
    ],
    expectedOutput: "['apple', 'banana']\n['carrot', 'pea']",
    expectedBehavior: 'Close the first list with a closing bracket before starting the second.',
  },
  'e-syn-4': {
    expectedCode: [
      'x = 10',
      'y = 20',
      '',
      'result = x + y',
      '',
      'print(result)',
    ],
    expectedOutput: '30',
    expectedBehavior: 'Use a variable name on the left side of the assignment.',
  },
  'e-run-1': {
    expectedCode: [
      'colors = ["red", "green", "blue"]',
      '',
      'print(colors[0])',
      'print(colors[1])',
      'print(colors[2])',
    ],
    expectedOutput: 'red\ngreen\nblue',
    expectedBehavior: 'Remove the line that accesses colors[3] since the list only has 3 items.',
  },
  'e-run-2': {
    expectedCode: [
      'person = {"name": "Alice", "age": 25}',
      '',
      'print(person["name"])',
      'print(person["age"])',
    ],
    expectedOutput: 'Alice\n25',
    expectedBehavior: 'Remove the lookup for "city" since it does not exist in the dictionary.',
  },
  'e-run-3': {
    expectedCode: [
      'text = "hello"',
      '',
      'print(text.upper())',
      'print(text.lower())',
      'print(text[::-1])',
    ],
    expectedOutput: 'HELLO\nhello\nolleh',
    expectedBehavior: 'Use slicing [::-1] instead of .reverse() to reverse a string.',
  },
  'e-run-4': {
    expectedCode: [
      'file = open("data.txt")',
      'content = file.read()',
      'print(content)',
    ],
    expectedOutput: '',
    expectedBehavior: 'Make sure the file exists before trying to open it.',
  },
  'e-run-5': {
    expectedCode: [
      'import math',
      'import statistics',
      '',
      'print(math.sqrt(16))',
    ],
    expectedOutput: '4.0',
    expectedBehavior: 'Remove the import of the module that does not exist.',
  },
  'e-run-6': {
    expectedCode: [
      'from datetime import datetime',
      'from os import path',
      '',
      'print(datetime.now())',
    ],
    expectedOutput: '2024-01-15 10:30:00',
    expectedBehavior: 'Remove the import from the package that does not have the needed module.',
  },
  'e-log-1': {
    expectedCode: [
      'count = 0',
      'for i in range(10):',
      '    count = i + 1',
      '',
      'print(f"Count: {count}")',
    ],
    expectedOutput: 'Count: 10',
    expectedBehavior: 'Add 1 to i when assigning to count so it reaches 10.',
  },
  'e-log-2': {
    expectedCode: [
      'x = 1',
      '',
      'while x > 0:',
      '    print(x)',
      '    x = x - 1',
    ],
    output: '1',
    expectedOutput: '1',
    expectedBehavior: 'Subtract 1 from x instead of adding so the loop eventually stops.',
  },
  'e-log-3': {
    expectedCode: [
      'for i in range(5):',
      '    print(f"Item {i}")',
      '',
      'print(f"Last: {i + 1}")',
    ],
    expectedOutput: 'Item 0\nItem 1\nItem 2\nItem 3\nItem 4\nLast: 5',
    expectedBehavior: 'Add 1 to i when printing the total to show the correct count.',
  },
  'e-log-4': {
    expectedCode: [
      'age = 20',
      'ticket = True',
      '',
      'if age >= 18 and ticket == True:',
      '    print("Enter")',
    ],
    expectedOutput: 'Enter',
    expectedBehavior: 'Use == for comparison instead of = in the condition.',
  },
  'e-log-5': {
    expectedCode: [
      'price = 10',
      'qty = 5',
      'tax = 2',
      '',
      'total = (price + qty) * tax',
      'print(f"Total: {total}")',
    ],
    expectedOutput: 'Total: 30',
    expectedBehavior: 'Add parentheses around (price + qty) so addition happens first.',
  },
  'bu-syn-1': {
    expectedCode: [
      'def filter_pos(data):',
      '    result = []',
      '    for x in data:',
      '        if x > 0:',
      '            result.append(x * 2)',
      '    return result',
      '',
      'print(filter_pos([1, -2, 3]))',
    ],
    expectedOutput: '[2, 6]',
    expectedBehavior: 'Add colons after the def and if statements, and fix the closing parenthesis.',
  },
  'bu-run-1': {
    expectedCode: [
      'data = {"a": 1, "b": 2}',
      '',
      'val = data["a"]',
      'result = val * 2',
      'text = str(result)',
    ],
    expectedOutput: '',
    expectedBehavior: 'Use a key that exists, avoid division by zero, and use a valid method.',
  },
  'bu-run-2': {
    expectedCode: [
      'def divide(a, b):',
      '    if b == 0:',
      '        return "Cannot divide by zero"',
      '    return a / b',
      '',
      'print(divide(10, 2))',
      'print(divide(10, 0))',
    ],
    expectedOutput: '5.0\nCannot divide by zero',
    expectedBehavior: 'Add a check for zero before dividing to handle the edge case.',
  },
  'bu-run-3': {
    expectedCode: [
      'user = input("Enter number: ")',
      'if user.isdigit():',
      '    num = int(user)',
      '    result = 100 / num',
      '    print(result)',
      'else:',
      '    print("Not a valid number")',
    ],
    expectedOutput: '',
    expectedBehavior: 'Check if the input is a digit before converting to int.',
  },
  'bu-log-1': {
    expectedCode: [
      'def search(arr, target):',
      '    for i in range(len(arr)):',
      '        if arr[i] == target:',
      '            return i',
      '    return -1',
      '',
      'print(search([10, 20, 30], 30))',
    ],
    expectedOutput: '2',
    expectedBehavior: 'The search algorithm works correctly for this case.',
  },
  'bu-log-2': {
    expectedCode: [
      'db = ["apple", "banana", "cherry"]',
      '',
      'def find(item):',
      '    return item.lower() in [x.lower() for x in db]',
      '',
      'print(find("Apple"))',
    ],
    expectedOutput: 'True',
    expectedBehavior: 'Convert both the query and database items to lowercase for comparison.',
  },
  'bu-log-3': {
    expectedCode: [
      'def check_age(age):',
      '    if 0 < age < 120:',
      '        return True',
      '    return False',
      '',
      'print(check_age(25))',
      'print(check_age(-5))',
      'print(check_age(150))',
    ],
    expectedOutput: 'True\nFalse\nFalse',
    expectedBehavior: 'Add upper and lower bounds to validate the age range.',
  },
  'bu-log-4': {
    expectedCode: [
      'def sort(arr):',
      '    n = len(arr)',
      '    for i in range(n):',
      '        for j in range(0, n-i-1):',
      '            if arr[j] > arr[j+1]:',
      '                arr[j], arr[j+1] = arr[j+1], arr[j]',
      '    return arr',
      '',
      'print(sort([64, 34, 25, 12]))',
    ],
    expectedOutput: '[12, 25, 34, 64]',
    expectedBehavior: 'The bubble sort algorithm works correctly.',
  },
  'bu-log-5': {
    expectedCode: [
      'def to_f(c):',
      '    return c * 9 / 5 + 32',
      '',
      'def to_c(f):',
      '    return (f - 32) * 5 / 9',
      '',
      'print(to_f(100))',
      'print(to_c(32))',
    ],
    expectedOutput: '212.0\n0.0',
    expectedBehavior: 'Fix the fahrenheit_to_celsius formula to use (f - 32) * 5 / 9.',
  },
  'bu-log-6': {
    expectedCode: [
      'def find_max(nums):',
      '    if not nums:',
      '        return None',
      '    m = nums[0]',
      '    for n in nums:',
      '        if n > m:',
      '            m = n',
      '    return m',
      '',
      'print(find_max([3, 7, 2, 8]))',
      'print(find_max([]))',
    ],
    expectedOutput: '8\nNone',
    expectedBehavior: 'Add a check for empty list before accessing nums[0].',
  },
};

export function getExpectedSolution(id) {
  return EXPECTED_SOLUTIONS[id] || null;
}

export default EXPECTED_SOLUTIONS;
